describe("validationMiddleware", () => {
  it.todo("throws VALIDATION_ERROR with fieldErrors containing only the first error for each field")
  it.todo("replaces req.body with the parsed data on success")
  it.todo("replaces req.params with the parsed data on success")
  it.todo("replaces req.query with the parsed data on success")
  it.todo("calls the next function on success")
})