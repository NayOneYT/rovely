describe("botService", () => {
  describe("checkLink", () => {
    it.todo("returns false for a non-existent telegramUserId")
    it.todo("returns true for an existing telegramUserId")
  })

  describe("saveLink", () => {
    it.todo("creates a new telegramLink in the DB on success")
  })
})